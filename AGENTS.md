# AGENTS.md

このリポジトリで作業するAIエージェント向けの制約です。

## 責務

- 責務は「自然言語で記述されたシナリオからティラノスクリプト（.ks）への変換」に限定する
- 音声入力を必須条件にしない。音声は入力方法の一つにすぎない
- 入力形式を固定しない。特定の書式を利用者に強制しない

## 作らないもの

- 独自ノベルゲームエンジンを作らない
- 独自DSL（独自シナリオ記法・独自中間形式・独自タグ）を作らない
- GUIを作らない
- 音声認識を実装しない
- 入力手段ごとの専用機能を先回りして作らない。音声固有の処理は、明確な必要性が確認されるまで追加しない
- ティラノ公式の既存機能を再実装しない

## 進め方

- 先回りして大規模化しない。まずSkillと例（`examples/`）を改善する
- Skillの改善は、サンプル入力と期待出力の差分をもとに行う
- コード追加は明確な必要性が生じた場合のみ行う（現時点で検査スクリプトの実装は必須ではない）

## 変換時の振る舞い

- 原文（作者が表現したセリフ、地の文、展開、分岐）を創作的に書き換えない
- 意図が不明な箇所を推測で補わない。コメントまたは確認事項として残す
- 詳細は [docs/conversion-policy.md](docs/conversion-policy.md) と [.claude/skills/scenario-to-tyrano/SKILL.md](.claude/skills/scenario-to-tyrano/SKILL.md) に従う
